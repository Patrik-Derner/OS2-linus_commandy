---------
| LAB A |
---------
su root
cd /     (možná)
mkdir ------     (vytvoří directory)
groupadd ------     (vytvoří groupy)
useradd -m -s /bin/bash -g groupname user     (vytvoří usery pod groupama)
chown groupAdmin:group folder
chmod 1770 filename
echo "This file contains confidentail information for the department > filePath/document.txt
chown groupAdmin:group fileName
chmod 740 folder/document.txt     (v případě)


<--- user creation lab, chtěl vypsat skupiny, uživatele a vidět práva adresáře --->
getent passwd / getent groups 

---------
| LAB B |
---------
nano user_management.sh

<--- script --->
#!/bin/bash

set_username(){
     printf "Enter a username:\n"
     read USERNAME

     if getent passwd "$USERNAME"; then
          printf "Username \'$USERNAME\' already exists. "
          set_username
     fi
} 

set_group(){
     printf "Enter a group:\n"
     read GROUP

     if getent group "$GROUP"; then
          printf "Group \'$GROUP\'  already exists. "
          set_group
     fi
}

set_username
set_group

sudo groupadd $GROUP; printf "$GROUP group created.\n"
sudo useradd -m -s /bin/bash -g $GROUP $USERNAME; printf "$USERNAME user created.\n"
sudo passwd $USERNAME
sudo mkdir /$USERNAME
sudo chown $USERNAME:$GROUP /$USERNAME
sudo chmod 1770 /$USERNAME

echo "Script Successfully Completed"
<--- script end --->

chmod +x ./user_management.sh
./user_management.sh

---------
| LAB C |
---------
cd
mkdir archive
mkdir backup
cd /var/log
tar cvf ~/archive/log.tar *.log
cd
tar tf ./archive/log.tar
cd backup/
tar xvf ~/archive/log.tar

---------
| LAB D |
---------
cat /etc/services
pwd
cut -d' ' -f1 /etc/services | cut -f1 | sort -u -b | grep '^[a-zA-]' > ./uniqueservices.txt && wc -l uniqueservices.txt
cat uniqueservices.txt
head uniqueservices.txt
tail uniqueservices.txt


